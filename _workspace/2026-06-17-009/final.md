# Hourglass + Music Box 추상화/플로우차트 v0.2

## 추상화 모델 한 줄 문장

특정 입력으로 감상 가능한 결과와 작동 가능량을 설정한 뒤, 일정 시간에 걸쳐 작동 가능량을 소모하며 사전에 정의된 시각적/청각적 결과를 완성하는 시스템.

## 추상화 모델을 이루는 요소

- A 트리거에 의해 감상할 결과 패턴이 선택된다.
- B 트리거에 의해 작동 가능량이 채워지거나 설정된다.
- C 트리거에 의해 작동 가능량 설정이 중단되고 결과 전개가 시작된다.
- 시간이 흐르면서 작동 가능량은 줄어들고 진행률은 증가한다.
- 진행률에 따라 사전에 정의된 시각적/청각적 결과가 함께 변화한다.
- 작동 가능량이 모두 소진되면 결과가 완성된 상태로 멈춘다.

## UI 상태

| 상태 | 설명 |
|---|---|
| 대기 | 감상할 결과 패턴을 아직 선택하지 않은 상태. |
| 설정 중 | 결과 패턴이 선택되고, 작동 가능량이 채워지거나 설정되는 상태. |
| 전개 중 | 작동 가능량이 줄어들고 진행률이 증가하면서 시각적/청각적 결과가 펼쳐지는 상태. |
| 일시정지 | 남은 작동 가능량과 현재 출력 상태를 유지한 채 전개를 멈춘 상태. |
| 완료 | 작동 가능량이 소진되어 사전에 정의된 결과가 완성된 상태. |

## 상태 전환 시나리오

1. 사용자가 감상할 결과 패턴을 선택하면 `predefined_result_rule`이 정해진다.
2. 사용자가 채우기 입력을 하면 `stored_potential`이 증가하거나 설정된다.
3. 사용자가 놓기 또는 시작 입력을 하면 `stored_potential`이 확정되고 전개 중 상태로 넘어간다.
4. 시간이 흐를 때마다 `remaining_potential`은 줄고 `progress_position`은 앞으로 이동한다.
5. `progress_position`이 바뀌면 시각적 출력과 청각적 출력이 `output_mapping_rule`에 맞춰 함께 변화한다.
6. 멈춤 입력이 발생하면 현재 출력 상태를 유지한 채 일시정지하고, 다시 시작 입력이 발생하면 전개를 이어간다.
7. `remaining_potential`이 `completion_threshold` 이하가 되면 완료 상태가 되고, 사용자가 초기화하면 다시 대기 상태로 돌아간다.

## Mermaid Flowchart

```mermaid
flowchart TD
  idle["대기<br/>결과 패턴 선택 대기"]
  choose["결과 선택<br/>사전 정의된 시각/청각 패턴 선택"]
  charge["설정 중<br/>작동 가능량 채워짐"]
  release{"놓기 / 시작<br/>트리거 발생?"}
  running["전개 중<br/>작동 가능량 감소<br/>시각/청각 결과 전개"]
  tick["시간 경과<br/>진행률 증가"]
  update["출력 갱신<br/>결과 패턴이 진행률에 맞춰 변화"]
  pause{"멈춤<br/>트리거 발생?"}
  paused["일시정지<br/>남은 가능량 보존<br/>출력 상태 유지"]
  resume{"다시 시작<br/>트리거 발생?"}
  done{"작동 가능량이<br/>소진되었는가?"}
  complete["완료<br/>사전 정의된 결과가 완성됨"]
  reset{"다시 감상 / 초기화<br/>트리거 발생?"}

  idle -->|패턴 선택| choose
  choose -->|채우기 입력| charge
  charge --> release
  release -->|아니오| charge
  release -->|예| running
  running --> tick
  tick --> update
  update --> pause
  pause -->|예| paused
  paused --> resume
  resume -->|아니오| paused
  resume -->|예| running
  pause -->|아니오| done
  done -->|아니오| running
  done -->|예| complete
  complete --> reset
  reset -->|아니오| complete
  reset -->|예| idle
```

<!-- HUMANIZE-SUMMARY
원본 글자수: 약 2300
윤문본 글자수: 약 2100
변경률: 약 16%
카테고리별 탐지: E-2 동일 종결 반복 -> 완화, F-4 명사화 반복 -> 일부 동사형으로 정리, H-3 메타 진입 표현 -> 제거
자체검증: 6/6 통과
등급: A
사유: 기존 CS 변수 중심 설명을 강사 예시 형식에 맞춰 한 줄 모델과 A-F 요소 중심으로 재정리함.
주요 변경:
- 추상화 한 줄 문장을 예시 형식에 맞게 재작성
- A-F 트리거/시간 경과 요소 추가
- 플로우차트를 대기, 설정 중, 전개 중, 일시정지, 완료 중심으로 단순화
-->
